[package]
name = "archlens-engine"
version = "0.0.1"
edition = "2021"
description = "High-performance analysis engine for ArchLens"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tree-sitter = "0.22"
tree-sitter-python = "0.21"
tree-sitter-go = "0.21"

[dev-dependencies]
